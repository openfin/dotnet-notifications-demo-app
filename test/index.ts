/**
 * Placeholder. The linter expects 'src' and 'test' directories, both containing at least one source file.
 *
 * Having this file here (and referencing it in tsconfig.json) allows the linter to run from service-tooling with no
 * modifications or exemptions required within the tooling.
 */
